# CAF v1.0 更新记录：Project Harness 最小闭环

时间：2026-06-27

## 本次更新

围绕 `v1.0_blueprint.md`，先落地最小运行闭环：

```text
需求输入
→ Project 创建
→ HarnessTask 创建
→ Artifact 注册
→ Stage Gate 审查
→ PASS 后可交付/归档
```

## 新增文件

```text
cabinet_framework/project_harness.py
docs/core/PROJECT_HARNESS.md
docs/core/ARTIFACT_REGISTRY.md
docs/core/GATE_SYSTEM.md
docs/core/WORKER_RUNTIME.md
docs/core/CONTEXT_MEMORY_ENGINE.md
docs/core/ORCHESTRATOR_SUPERVISOR.md
tests/test_project_harness_v1.py
```

## CLI 新增命令

```bash
python3 -m cabinet_framework.cli harness-project
python3 -m cabinet_framework.cli harness-task
python3 -m cabinet_framework.cli harness-artifact
python3 -m cabinet_framework.cli harness-gate
python3 -m cabinet_framework.cli harness-status
python3 -m cabinet_framework.cli harness-demo
```

## Runtime 产物

```text
runtime/project_harness/projects.json
runtime/project_harness/tasks/*.json
runtime/project_harness/artifacts.jsonl
runtime/project_harness/gate_events.jsonl
runtime/project_harness/demo/minimal_loop.md
```

## 验证

命令：

```bash
cd <repo-root>
python3 -m unittest tests.test_project_harness_v1 -v
python3 -m cabinet_framework.cli harness-demo
python3 -m cabinet_framework.cli harness-status
python3 -m cabinet_framework.cli gate
```

结果：

```text
Ran 3 tests in 0.008s
OK

harness-demo: gate status pass
caf gate: PASS
```

## 遗留问题

v1.0 还没完成：

1. Worker Runtime 还只是协议文档，未真正接入 worker 执行器。
2. Gate System 当前是最小规则，尚未接入 hooks/lifecycle。
3. Artifact Registry 尚未接 dashboard。
4. Context / Memory Engine 尚未和任务召回深度绑定。
5. Orchestrator / Supervisor 尚未自动把普通需求拆成 HarnessTask。

## 下一步

继续 v1.0 第二步：

> 把现有 worker / task lifecycle 接入 Project Harness，让普通 intake 生成 HarnessTask，并让 worker 产物必须登记 Artifact 后才能 gate pass。
