# Claude Code Adapter

Status: portable project-instructions path.

Claude Code should read `AGENTS.md` or an exported `CLAUDE.md`, then execute task packets in `runtime/tasks/`. Claude-specific hooks are adapter implementations, not framework core.
