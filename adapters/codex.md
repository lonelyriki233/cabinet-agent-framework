# Codex Adapter

Status: portable task-packet path.

Codex should read `AGENTS.md`, `README.md`, and `runtime/tasks/*.json`. Exact CLI invocation is intentionally not hard-coded; install-specific wrappers can implement `cabinet_framework.adapters.HostAdapter`.
