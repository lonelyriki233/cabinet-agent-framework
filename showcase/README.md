# CAF Showcase

CAF v1.0 的动态展示页。

技术栈：

- Vite
- React
- TypeScript
- Framer Motion
- Lucide React

运行：

```bash
cd showcase
npm install
npm run dev
```

构建：

```bash
npm run build
```

定位：

这不是 dashboard，不负责运行时管理；这是介绍 CAF v1.0 框架内容和能力闭环的展示页。
