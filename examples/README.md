# Examples

Run the 30-second local demo:

```bash
python3 examples/demo_30s.py
```

The demo resets runtime state, registers one portable framework task, builds the keyword index, runs gate checks, and prints dashboard instructions.
